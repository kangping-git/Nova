// フィボナッチ数列を計算するプログラム

func fibonacci(n: int) -> int {
    if n == 0 || n == 1 {
        return n
    } else {
        return fibonacci(n - 1) + fibonacci(n - 2)
    }
}

func main() {
    let n = 10
    for i in 0..n {
        println("Fibonacci of ", i, " is ", fibonacci(i))
    }
}